import { strict as assert } from "node:assert";
import { readFileSync } from "node:fs";
import { defaultStateTriggerValue, stateActionReferencePatch, stateActionStates, stateTriggerReferencePatch, stateTriggerRelations } from "./SceneLogicEditing";
import { sceneLogicInsertMenuCapabilities } from "./SceneLogicInsertMenu";
import type { SceneLogicDocumentV1 } from "../game/scene-logic/SceneLogicDocument";
const source=readFileSync(new URL("./PixelBgrLabUI.ts",import.meta.url),"utf8");
assert.match(source,/sceneContentsLogicRow/);assert.match(source,/dataset\.inspector="logic"/);assert.doesNotMatch(source,/sceneContentsRow\("TRI"/);assert.match(source,/sceneContentsRow\("EVE"/);
assert.match(source,/this\.select\(trigger\.markerId,logic\.spaces\.markers\.map/);assert.match(source,/document\.createTextNode\("cross"\)/,"Marker relation remains read-only");
assert.match(source,/this\.select\(trigger\.rangeId,logic\.spaces\.ranges\.map/);assert.match(source,/this\.select\(trigger\.zoneId,logic\.spaces\.zones\.map/);assert.equal(source.match(/\["enter","inside","exit"\]/g)?.length,2,"Range and Zone expose all relations");
assert.match(source,/this\.select\(trigger\.stateId,logic\.states\.map/);assert.match(source,/stateTriggerReferencePatch\(nextState,trigger\.relation,trigger\.value\)/);assert.match(source,/state\?\.valueType==="boolean"\?check/);assert.match(source,/state\?\.valueType==="number"\?num/);assert.match(source,/:text\(String\(trigger\.value\)/);
assert.deepEqual(stateTriggerRelations("number"),["==","!=","<","<=",">",">="]);assert.deepEqual(stateTriggerRelations("boolean"),["==","!="]);assert.deepEqual(stateTriggerRelations("string"),["==","!="]);assert.equal(defaultStateTriggerValue("boolean"),false);assert.equal(defaultStateTriggerValue("number"),0);assert.equal(defaultStateTriggerValue("string"),"");assert.deepEqual(stateTriggerReferencePatch({id:"bool",address:"player.alive",valueType:"boolean"},">",3),{stateId:"bool",relation:"==",value:false});
const emptyLogic={version:1,spaces:{markers:[],ranges:[],zones:[]},states:[],triggers:[],events:[],actions:[],triggerEventBindings:[],eventActionBindings:[]} satisfies SceneLogicDocumentV1;
const noStates=sceneLogicInsertMenuCapabilities(emptyLogic);assert.equal(noStates.canCreateStateTrigger,false);assert.equal(noStates.canCreateStateSetAction,false);assert.equal(noStates.canCreateStateNumericAction,false);assert.deepEqual(noStates.stateReferencePresets.map(preset=>preset.address),["scene.scrollSpeed","player.alive","player.shield"]);
const booleanOnly=sceneLogicInsertMenuCapabilities({...emptyLogic,states:[{id:"state_player_alive",address:"player.alive",valueType:"boolean"}]});assert.equal(booleanOnly.canCreateStateTrigger,true);assert.equal(booleanOnly.canCreateStateSetAction,true);assert.equal(booleanOnly.canCreateStateNumericAction,false);assert.deepEqual(booleanOnly.stateReferencePresets.map(preset=>preset.address),["scene.scrollSpeed","player.shield"]);
const withNumber=sceneLogicInsertMenuCapabilities({...emptyLogic,states:[{id:"state_player_alive",address:"player.alive",valueType:"boolean"},{id:"state_scroll_speed",address:"scene.scrollSpeed",valueType:"number"}]});assert.equal(withNumber.canCreateStateTrigger,true);assert.equal(withNumber.canCreateStateSetAction,true);assert.equal(withNumber.canCreateStateNumericAction,true);assert.deepEqual(withNumber.stateReferencePresets.map(preset=>preset.address),["player.shield"]);
assert.match(source,/stateActionStates\(action\.type,logic\.states\)/);assert.match(source,/updateAction\(scene,action\.id,stateActionReferencePatch/);assert.match(source,/action\.type==="set"&&state\?\.valueType==="boolean"\?check/);assert.match(source,/action\.type==="set"&&state\?\.valueType==="string"\?text/);assert.match(source,/:num\(Number\(action\.value\),1/);
const actionStates=[{id:"number",address:"scene.scrollSpeed",valueType:"number"},{id:"boolean",address:"player.alive",valueType:"boolean"},{id:"string",address:"scene.label",valueType:"string"}] as const;assert.deepEqual(stateActionStates("set",actionStates).map(x=>x.id),["number","boolean","string"]);assert.deepEqual(stateActionStates("increment",actionStates).map(x=>x.id),["number"]);assert.deepEqual(stateActionStates("decrement",actionStates).map(x=>x.id),["number"]);assert.deepEqual(stateActionReferencePatch("set",actionStates[0],true),{stateId:"number",value:0});assert.deepEqual(stateActionReferencePatch("set",actionStates[1],true),{stateId:"boolean",value:true});assert.deepEqual(stateActionReferencePatch("increment",actionStates[0],Number.NaN),{stateId:"number",value:1});
console.log("Scene Lab Logic authoring UI smoke passed");
