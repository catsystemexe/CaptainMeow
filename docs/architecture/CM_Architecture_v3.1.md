# Captain Meow – Unified Engine Architecture v3.1

> **Status: HISTORICAL / SUPERSEDED AS GLOBAL AUTHORITY**
>
> This early architecture note preserves important deterministic-engine principles, but it is no longer the repository's Single Source of Truth. For current architecture, use `docs/project/01-architecture.md`, current code/runtime contracts, and `AGENTS.md` for repository-local invariants.

Historical scope:
- Deterministic heartbeat (60Hz)
- EventBus with phase ownership
- Entity lifecycle with AoS + generation
- Debug & replay scope

The original statement that implementation must conform strictly to this document is superseded by the current authority model.
